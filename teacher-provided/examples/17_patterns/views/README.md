The __views__ directory contains classes that implement the user interface. These classes can access the model to
display data, but are not allowed to modify the model. Modifying the model must be done through a controller.
